EDITING:
commit in github.dev (change com to dev in github file)
pull changes in repl.it to view site

PRIORITY:
1. change lists to json structures using existing musicbrainz database? (keep all.txt and artists.txt)
    group name
        gender (f/m/coed)
        start year active (num, self-entry)
        end year active (num/null, self-entry) null=presently active
        type (solo/group)
        nugu (t/f)
        popular (t/f)
        last.fm (array of usernames)
    idea: iterate in artists.txt, for each element, find in musicbrainz: gender, type, start/end years (for groups) (skip undefined elements)
2. change buttons to tick boxes, release shown must fulfill all checked requirements
    two text inputs for year range e.g. (____)-(____) but in html
3. decades: check that release year of release matches chosen decade

NEXT:
1. history list for button presses: append results to array and display
2. playlist maker: button adds current song to array >> can then export to spotify/youtube playlist
    look up spotify track ids in bulk >> interate through array
3. narrow decades to specific year

REMINDERS:
cd <this-folder>
git add .
git commit -am "message"
git push origin HEAD:<branch-name>
